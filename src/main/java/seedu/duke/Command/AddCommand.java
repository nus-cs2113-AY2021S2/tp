package seedu.duke.Command;

import java.util.List;

public abstract class AddCommand extends Command{
    private final String FEEDBACK_FORMAT = "A new %s record is added successfully!\nRecord summary:%s";
    /*
    public AddCommand(String type, List<String> params){
        switch (type){
        case "E":

            feedback = String.format(FEEDBACK_FORMAT,"exercise activity", );
        case "S":
        }
    }

     */
}
